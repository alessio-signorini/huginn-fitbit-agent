<% if @agent.memory['oauth2_token'].present? %>
  Agent has been authorized for the following scopes:
  <ul>
  <% @agent.memory['oauth2_token']['scope'].split(' ').each do |scope| %>
    <li><%=scope%></li>
  <% end %>
  </ul>
<% else %>
  Please complete OAUTH2, specify <b>only</b> the following callback URL in your Personal Fitbit app
  <p>
    <pre><%= @agent.web_request_url(:oauth2_callback, request.base_url) %></pre>
  </p>
  and then click
  <a href="<%=@agent.fitbit_authorize_url%>">this link</a>
  to authorize it to fetch data for your account.
<% end %>
